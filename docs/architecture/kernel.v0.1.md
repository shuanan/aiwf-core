# AIWF Kernel v0.1

The kernel is the always-loaded minimum governance core.

It must stay thin enough to place in `AGENTS.md`, `CLAUDE.md`, or a downstream adapter block.

The kernel does not include hooks, skills, full SOPs, lifecycle flows, or implementation details.

See `aiwf/kernel/kernel.v0.1.yaml`.
